name: Deploy curriculo on Curriculo Branch
  uses: andstor/copycat-action@v3
  with:
    personal_token: ${{ secrets.REPO_TOKEN }}
    src_path: curriculo
    dst_path: /
    dst_owner: murilogteixeira
    dst_repo_name: curriculo
